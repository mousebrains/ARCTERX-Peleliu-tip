# Core analysis. Python 3.10+.
numpy>=1.24
scipy>=1.10
netCDF4>=1.6
matplotlib>=3.7

# Only needed to rebuild data/ from the SeaChest archive
# (tools/make_data_subset.py also uses scipy.io for the C05 .mat).
